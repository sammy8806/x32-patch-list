{
  "name": "x32-patch-list",
  "version": "2.0.0",
  "description": "Generate printable patch lists from X32 scene files — offline-capable single page app",
  "license": "GPL-2.0-only",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run dev.ts",
    "build": "bun run build.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.6.3"
  }
}
